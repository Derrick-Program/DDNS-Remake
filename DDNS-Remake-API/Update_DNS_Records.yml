info:
  name: Update_DNS_Records
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{base}}/dns_records/:record_id"
  params:
    - name: record_id
      value: e7641916-3e7b-4392-a5ec-2748eaf51299
      type: path
  body:
    type: json
    data: |-
      {
        "Ip": "127.0.0.1"
      }
  auth:
    type: bearer
    token: "{{api}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
