info:
  name: Get_DNS_Records
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/dns_records/:device_id"
  params:
    - name: device_id
      value: bac8c9e4-5d65-48c9-96f9-408ddd4d45ab
      type: path
  auth:
    type: bearer
    token: "{{api}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
