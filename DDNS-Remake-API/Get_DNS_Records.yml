info:
  name: Get_DNS_Records
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/dns_records"
  auth:
    type: bearer
    token: "{{api}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
