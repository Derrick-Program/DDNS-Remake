[package]
name = "ddns-core"
version = "0.1.0"
edition = "2024"

[dependencies]
# salvo = { workspace = true, features = ["oapi"] }
salvo-oapi = "0.89.2"
serde.workspace = true
chrono.workspace = true
